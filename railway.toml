# Railway configuration for TradIA backend
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend.api.api_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
